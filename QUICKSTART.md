# 🚀 SMARTLARK - Быстрый старт

Краткое руководство для начала работы с SMARTLARK архиватором v2.0.

## 📥 Установка

1. Скачайте `SMARTLARK.exe`
2. Поместите файл в любую папку (например, `C:\SMARTLARK\`)
3. Для удобства добавьте папку в PATH или используйте полный путь

## ✅ Проверка установки

```bash
SMARTLARK
```

Если всё правильно, вы увидите справку по использованию.

## 🎯 Основные команды

### 1. Создание архива

```bash
# Создать архив и добавить файлы
SMARTLARK a myarchive.lark file1.txt file2.doc

# Добавить несколько файлов
SMARTLARK a myarchive.lark *.txt *.doc

# Добавить все файлы из папки (рекурсивно)
SMARTLARK a myarchive.lark *.* -r
```

### 2. Просмотр содержимого

```bash
# Показать список файлов в архиве
SMARTLARK l myarchive.lark
```

Вы увидите:
- Размеры файлов (оригинальный и сжатый)
- Коэффициент сжатия
- Метод сжатия
- Дату и время
- Подробную статистику архива

### 3. Извлечение файлов

```bash
# Извлечь все файлы
SMARTLARK x myarchive.lark

# Извлечь в конкретную папку
SMARTLARK x myarchive.lark -o C:\extracted\

# Извлечь только текстовые файлы
SMARTLARK x myarchive.lark *.txt -o C:\extracted\
```

### 4. Проверка целостности

```bash
# Проверить архив на ошибки
SMARTLARK t myarchive.lark
```

### 5. Удаление файлов

```bash
# Удалить файл из архива
SMARTLARK d myarchive.lark file1.txt

# Удалить все временные файлы
SMARTLARK d myarchive.lark *.tmp
```

## ⚙️ Полезные опции

### Выбор метода сжатия

```bash
# Без сжатия (для уже сжатых файлов)
SMARTLARK a archive.lark file.zip -m store

# LZSS (быстрое сжатие)
SMARTLARK a archive.lark file.txt -m lzss

# LZHUF (по умолчанию, хорошее сжатие)
SMARTLARK a archive.lark file.txt -m lzhuf

# DEFLATE (максимальное сжатие)
SMARTLARK a archive.lark file.txt -m deflate -c 9

# LZW (для повторяющихся данных)
SMARTLARK a archive.lark file.txt -m lzw

# LZ77 (классический алгоритм)
SMARTLARK a archive.lark file.txt -m lz77
```

### Уровень сжатия

```bash
# Быстрое сжатие (уровень 1)
SMARTLARK a archive.lark file.txt -c 1

# Сбалансированное (уровень 5)
SMARTLARK a archive.lark file.txt -c 5

# Максимальное сжатие (уровень 9)
SMARTLARK a archive.lark file.txt -c 9
```

### Подробный режим

```bash
# Показать детальную статистику операций
SMARTLARK a archive.lark file.txt -v
```

Вы увидите:
- Время выполнения операции
- Скорость обработки (МБ/с)
- Размер обработанных данных

## 📝 Практические примеры

### Пример 1: Резервное копирование документов

```bash
# Создать архив всех документов
SMARTLARK a backup_2025.lark *.doc *.docx *.pdf -r -v

# Проверить архив
SMARTLARK t backup_2025.lark

# Просмотреть содержимое
SMARTLARK l backup_2025.lark
```

### Пример 2: Архивирование логов

```bash
# Создать архив всех лог-файлов
SMARTLARK a logs.lark *.log -r -m deflate -c 9

# Извлечь только старые логи
SMARTLARK x logs.lark old_*.log -o C:\old_logs\
```

### Пример 3: Очистка архива

```bash
# Удалить все временные файлы из архива
SMARTLARK d archive.lark *.tmp *.bak

# Проверить результат
SMARTLARK l archive.lark
```

### Пример 4: Обновление файлов

```bash
# Обновить файл в архиве (перезаписать)
SMARTLARK u archive.lark updated_file.txt
```

## 🎨 Использование масок (wildcards)

SMARTLARK поддерживает маски `*` и `?` во всех командах:

```bash
# * - любое количество символов
SMARTLARK x archive.lark *.txt          # Все .txt файлы
SMARTLARK x archive.lark test*.*         # Все файлы, начинающиеся с "test"
SMARTLARK d archive.lark backup*.*        # Удалить все backup файлы

# ? - один символ
SMARTLARK x archive.lark file?.txt        # file1.txt, file2.txt, fileA.txt
SMARTLARK x archive.lark doc?.*           # doc1.*, doc2.*, docA.*
```

## 🔍 Часто задаваемые вопросы

### Как создать архив с максимальным сжатием?

```bash
SMARTLARK a archive.lark files.txt -m deflate -c 9 -v
```

### Как быстро добавить файлы без сжатия?

```bash
SMARTLARK a archive.lark files.zip -m store
```

### Как извлечь только определенные файлы?

```bash
SMARTLARK x archive.lark *.txt -o extracted\
```

### Как проверить архив перед использованием?

```bash
SMARTLARK t archive.lark
```

### Как посмотреть подробную статистику?

```bash
SMARTLARK l archive.lark
```

Команда `list` автоматически показывает:
- Общую статистику архива
- Статистику по методам сжатия
- Информацию о размере архива

## 📊 Сравнение методов сжатия

| Метод | Скорость | Сжатие | Рекомендация |
|-------|----------|--------|--------------|
| **Store** | ⚡⚡⚡ | ❌ | Уже сжатые файлы (ZIP, JPG) |
| **LZSS** | ⚡⚡⚡ | ⭐⭐ | Текстовые файлы, быстро |
| **LZHUF** | ⚡⚡ | ⭐⭐⭐ | Универсальный (по умолчанию) |
| **DEFLATE** | ⚡ | ⭐⭐⭐⭐ | Максимальное сжатие |
| **LZW** | ⚡⚡ | ⭐⭐⭐ | Повторяющиеся данные |
| **LZ77** | ⚡⚡ | ⭐⭐ | Классический алгоритм |

## 💡 Советы по использованию

1. **Для текстовых файлов**: Используйте `-m lzhuf` или `-m deflate`
2. **Для уже сжатых файлов**: Используйте `-m store` (быстрее)
3. **Для максимального сжатия**: Используйте `-m deflate -c 9`
4. **Для быстрого сжатия**: Используйте `-m lzss -c 1`
5. **Всегда проверяйте архив**: Используйте `SMARTLARK t archive.lark` после создания

## 🧪 Тестирование

Если хотите проверить работу архиватора:

```bash
# Запустить все тесты
SMARTLARK test all

# Запустить тесты конкретного алгоритма
SMARTLARK test lzss
SMARTLARK test deflate
SMARTLARK test archive
```

## 📚 Дополнительная информация

- **Полная документация**: См. `README_EN.md` или `README_RU.md`
- **Аудит кода**: См. `CODE_AUDIT_REPORT.md` и `AUDIT_FINAL_SUMMARY.md`
- **Справка в программе**: `SMARTLARK -h`
- **Формат архива**: См. `SMARTLARK_Format_Specification_v2.0.md`

## 🆘 Получение помощи

```bash
# Показать справку
SMARTLARK -h

# Или просто запустить без параметров
SMARTLARK
```

---

**Готово!** Теперь вы можете использовать SMARTLARK для работы с архивами.

**Следующий шаг**: Попробуйте создать свой первый архив!

```bash
SMARTLARK a my_first_archive.lark your_file.txt
```

